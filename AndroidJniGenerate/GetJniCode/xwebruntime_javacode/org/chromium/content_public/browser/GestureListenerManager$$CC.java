package org.chromium.content_public.browser;

import org.chromium.content.browser.GestureListenerManagerImpl;

public abstract class GestureListenerManager$$CC {
    public static GestureListenerManager fromWebContents$$STATIC$$(WebContents arg0) {
        return GestureListenerManagerImpl.fromWebContents(arg0);
    }
}

